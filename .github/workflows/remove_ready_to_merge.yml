name: Remove "ready to merge" label

on:
  pull_request_target:
    types: [closed]
  workflow_call:


permissions:
  pull-requests: write

jobs:
  remove_label:
    name: Remove ready to merge label
    uses: napari/napari/.github/workflows/remove_ready_to_merge.yml@59ea1849d144e53cc9d42826139f545937b3eb91 # v0.6.6
